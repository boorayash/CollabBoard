# Plan: 02-03 - Premium Kanban Board

**Goal**: Transform the board interface combining solid readability, selective glassmorphism, and Framer Motion layouts.

## Context
Reflecting user's strict instructions: Glass ONLY on list columns, NOT on task cards. Solid task cards for readability. Subtle hover effects only (no tilt). Use `layout` prop for D&D. Add empty states.

## Tasks

### 1. Glass Columns & Highlight
<action>
- Modify `client/src/components/ListColumn.jsx`.
- Update the `<Paper>` container to use the glass CSS variables instead of solid `#1e1e1e`.
- Check `dnd-kit`'s active drag state (using `useDndContext` or props) to detect if a card is currently being dragged over this column. Add a dynamic border highlight (e.g., `border: 1px solid var(--color-primary)`) when active.
- If `list.cards.length === 0`, display a clean empty state: "No tasks yet" inside the list.
</action>
<read_first>
- `client/src/components/ListColumn.jsx`
</read_first>
<acceptance_criteria>
- `ListColumn.jsx` container applies `var(--glass-bg)` and `var(--glass-blur)`.
- It renders the text "No tasks yet" when cards length is 0.
</acceptance_criteria>

### 2. Solid Task Cards & Layout Animation
<action>
- Modify `client/src/components/TaskCard.jsx`.
- Wrap the card in `<motion.div layout>`. This is advanced Framer Motion UI that automatically transitions physical DOM movement.
- Apply a solid, dark background (e.g., `#1e293b` or `#2d3748`) to ensure text readability against the complex global background.
- Apply `whileHover={{ scale: 1.03 }}` (SUBTLE interaction, no tilt as requested).
</action>
<read_first>
- `client/src/components/TaskCard.jsx`
</read_first>
<acceptance_criteria>
- `TaskCard.jsx` returns a `<motion.div layout>`.
- `TaskCard.jsx` hover animation explicitly uses `scale: 1.03` max.
</acceptance_criteria>

### 3. Board Empty State
<action>
- Modify `client/src/pages/Board.jsx`.
- When a board has no lists (`board.lists.length === 0`), upgrade the center text to an explicit call-to-action: "Create your first list".
- Upgrade the "Add List" button to look premium (e.g., matching the `--color-secondary` or dashed border with glow on hover).
</action>
<read_first>
- `client/src/pages/Board.jsx`
</read_first>
<acceptance_criteria>
- `Board.jsx` displays the text "Create your first list" when lists are empty.
</acceptance_criteria>
