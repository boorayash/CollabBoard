# Plan: 02-04 - UX Polish & Loaders

**Goal**: Finalize the user experience with professional loading states, crisp button feedback, and lucide-react icons.

## Context
Reflecting the final user requests: replace blank screens with skeleton loaders, add proper button interactions, and polish remaining UI corners.

## Tasks

### 1. Skeleton Loaders
<action>
- Inside `client/src/pages/Board.jsx` (and child components), replace the `<CircularProgress />` with a composed Skeleton loader that matches the new structure (Sidebar block + vertical column blocks).
- Use `MUI Skeleton` component with appropriate `variant` (rectangular, circular) and adjust the background color to match the dark theme (`#1e293b`).
</action>
<read_first>
- `client/src/pages/Board.jsx`
</read_first>
<acceptance_criteria>
- `<Skeleton>` component from MUI is used instead of `<CircularProgress>`.
- The skeleton shape mimics a sidebar and at least 3 columns.
</acceptance_criteria>

### 2. Smooth Scrolling & Scrollbar Reset
<action>
- Within the `ListColumn`'s `dnd-kit` `<Box>` and `KanbanBoard`'s main horizontal flow, ensure CSS `overflow-x: auto` is used, while hiding the scrollbars for a clean look. Scrollbar hiding should have been implemented in 02-01, verify it works smoothly for horizontal dragging.
</action>
<read_first>
- `client/src/components/KanbanBoard.jsx`
- `client/src/components/ListColumn.jsx`
</read_first>
<acceptance_criteria>
- The container for lists has `overflowX: 'auto'` allowing horizontal scrolling when columns overflow the screen.
</acceptance_criteria>

### 3. Iconography & Button Feedback
<action>
- Install `lucide-react` if not done in 02-01.
- Replace any generic buttons in the Kanban board (like "+ Add List", "+ Add Task") with buttons containing a `<Plus />` icon from `lucide-react`.
- Verify every primary button globally has a distinct hover state, active/click scale effect (`&:active { transform: scale(0.98); }`), and visually distinct disabled state.
</action>
<read_first>
- `client/src/components/ListColumn.jsx`
- `client/src/pages/Board.jsx`
</read_first>
<acceptance_criteria>
- `lucide-react` is imported.
- `Plus` icon is rendered next to "Add List" and "Add Task" buttons.
</acceptance_criteria>
