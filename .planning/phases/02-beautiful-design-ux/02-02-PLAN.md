# Plan: 02-02 - Navigation & Auth

**Goal**: Implement the new Sidebar layout and elevate the Authentication experience using glassmorphism.

## Context
Reflecting user's instruction: Sidebar should use glassmorphism and include Board list, User Profile, and Logout. The `Board.jsx` layout changes to `[ Sidebar ] [ Board Content ]`. 

## Tasks

### 1. New Sidebar Component
<action>
- Create `client/src/components/Sidebar.jsx`.
- Build a fixed-width (e.g., `250px`) or flex column using the glass CSS variables: `background: var(--glass-bg)`, `border-right: 1px solid var(--glass-border)`, `backdrop-filter: blur(var(--glass-blur))`.
- Iterate over user's existing teams/boards (from Redux) and display them in a list.
- Add a user profile section at the bottom containing their name and a stylized Logout button (with hover effects and `lucide-react` icon).
</action>
<read_first>
- `client/src/pages/Board.jsx`
- `client/src/store/slices/boardSlice.js`
</read_first>
<acceptance_criteria>
- `Sidebar.jsx` applies `var(--glass-bg)` and `var(--glass-blur)`.
- It renders a list of boards and a logout button.
</acceptance_criteria>

### 2. Board Layout Restructure
<action>
- Modify `client/src/pages/Board.jsx`.
- Remove the top horizontal Header.
- Change the root container structure to `display: 'flex', flexDirection: 'row'`.
- Render `<Sidebar />` on the left.
- Render the Kanban board area on the right taking `flexGrow: 1`.
</action>
<read_first>
- `client/src/pages/Board.jsx`
</read_first>
<acceptance_criteria>
- `Board.jsx` imports and renders `<Sidebar />`.
- `Board.jsx` root container uses `flexDirection: 'row'`.
</acceptance_criteria>

### 3. Authenticate Screens
<action>
- Modify `client/src/pages/Login.jsx` and `client/src/pages/Signup.jsx`.
- Wrap the main form card in `<motion.div>` with `initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }}`.
- Apply glass CSS variables to the form container.
- Update `TextField` or standard inputs to have a solid background (`#1e1e1e`) but add a `:focus` glow (`box-shadow: 0 0 10px var(--color-primary)` or similar).
</action>
<read_first>
- `client/src/pages/Login.jsx`
- `client/src/pages/Signup.jsx`
</read_first>
<acceptance_criteria>
- `Login.jsx` form container uses `<motion.div>`.
- Inputs have a distinct focus outline/glow defined in CSS or inline styles matching primary/secondary themes.
</acceptance_criteria>
